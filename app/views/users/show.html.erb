<h1>Profile</h1>
<h2> User: <%= @user.name %> </h2>
<% check = 0 %>
<h3> Events User is attending </h3>
<p>
    <ul>
        <li> Past Events </li>
        <% @attendeds.each do |attended| %> 
            <% if attended.eventdate > Date.today and check == 0 %>
                    <p> Future events </p>
                    <% check = 1 %>
            <% end %>
            <li>
                <%= attended.description %>
                <%= attended.eventdate %>
            </li>
        <% end %>
    </ul>
</p>

<%= link_to "All events ", events_path %>